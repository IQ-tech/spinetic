import "../styles.stories.scss";
import { Meta, Title, Primary, Controls, Stories } from "@storybook/blocks";
import { argTypes } from "../argTypes.ts";

import { GoToTheTop } from "../CardExample.tsx";

<GoToTheTop />

<Meta title="pages/Props" />

# Spinetic
<br />

## Props

## `children`: _ReactNode_ or _ReactNode[]_

Children elements to be rendered inside the Spinetic component. It can be a single `ReactNode` or an _array_ of `ReactNodes`.

## `config`: _TypesConfigOptional_

The `config` prop accepts an object with optional settings for customizing the Spinetic component. To run tests and explore detailed information about each property and its functionality, navigate by clicking on individual properties and experiment with them in the playground:

```tsx
export interface TypesConfigOptional {
  arrows?: boolean;
  arrowsStyle?: TypesDefaultArrowsStyle;
  hideArrows?: boolean;

  progressIndicator?: boolean;
  dots?: boolean;
  dotsStyle?: TypesDefaultDotsStyle,
  itemNumberInDot?: boolean;

  autoRotate?: boolean;
  msPerAutoRotate?: number;

  clickTransitionCtrl?: boolean;
  msPerClicks?: number;

  draggable?: boolean;
  touchThreshold?: number;

  layout?: TypesLayout,
  showItems?: number;
  autoWidth?: boolean;
  fullHeightItems?: boolean;

  groupScroll?: boolean;
  groupItemsScroll?: number;

  responsive?: TypesReponsiveSettings[];
}

interface TypesReponsiveSettings {
  breakpoint: number;
  settings: TypesConfigOptional;
}
```

- [**arrows**](?path=/docs/pages-playground--documentation#arrows): <span dangerouslySetInnerHTML={{ __html:argTypes.arrows.description}} />
- [arrowsStyle](?path=/docs/pages-playground--documentation#arrowsStyle): <span dangerouslySetInnerHTML={{ __html:argTypes.arrowsStyle.description}} />
- [hideArrows](?path=/docs/pages-playground--documentation#hideArrows): <span dangerouslySetInnerHTML={{ __html:argTypes.hideArrows.description}} />

- [progressIndicator](?path=/docs/pages-playground--documentation#progressIndicator): <span dangerouslySetInnerHTML={{ __html:argTypes.progressIndicator.description}} />
- [dots](?path=/docs/pages-playground--documentation#dots): <span dangerouslySetInnerHTML={{ __html:argTypes.dots.description}} />
- [dotsStyle](?path=/docs/pages-playground--documentation#dotsStyle): <span dangerouslySetInnerHTML={{ __html:argTypes.dotsStyle.description}} />
- [itemNumberInDot](?path=/docs/pages-playground--documentation#itemNumberInDot): <span dangerouslySetInnerHTML={{ __html:argTypes.itemNumberInDot.description}} />

- [autoRotate](?path=/docs/pages-playground--documentation#autoRotate): <span dangerouslySetInnerHTML={{ __html:argTypes.autoRotate.description}} />
- [msPerAutoRotate](?path=/docs/pages-playground--documentation#msPerAutoRotate): <span dangerouslySetInnerHTML={{ __html:argTypes.msPerAutoRotate.description}} />

- [draggable](?path=/docs/pages-playground--documentation#draggable): <span dangerouslySetInnerHTML={{ __html:argTypes.draggable.description}} />
- [touchThreshold](?path=/docs/pages-playground--documentation#touchThreshold): <span dangerouslySetInnerHTML={{ __html:argTypes.touchThreshold.description}} />

- [showItems](?path=/docs/pages-playground--documentation#showItems): <span dangerouslySetInnerHTML={{ __html:argTypes.showItems.description}} />
- [fullHeightItems](?path=/docs/pages-playground--documentation#fullHeightItems): <span dangerouslySetInnerHTML={{ __html:argTypes.fullHeightItems.description}} />

- [autoWidth](?path=/docs/pages-playground--documentation#autoWidth): <span dangerouslySetInnerHTML={{ __html:argTypes.autoWidth.description}} />
- [centerSingleItemAW](?path=/docs/pages-playground--documentation#centerSingleItemAW): <span dangerouslySetInnerHTML={{ __html:argTypes.centerSingleItemAW.description}} />

- [groupScroll](?path=/docs/pages-playground--documentation#groupScroll): <span dangerouslySetInnerHTML={{ __html:argTypes.groupScroll.description}} />
- [groupItemsScroll](?path=/docs/pages-playground--documentation#groupItemsScroll): <span dangerouslySetInnerHTML={{ __html:argTypes.groupItemsScroll.description}} />

- [clickTransitionCtrl](?path=/docs/pages-playground--documentation#clickTransitionCtrl): <span dangerouslySetInnerHTML={{ __html:argTypes.clickTransitionCtrl.description}} />
- [msPerClicks](?path=/docs/pages-playground--documentation#msPerClicks): <span dangerouslySetInnerHTML={{ __html:argTypes.msPerClicks.description}} />

## `change`: _function_

Callback function triggered on a change event in the Spinetic component. The `change` function is called every time an item in the carousel is changed, providing information about the previous and current state.

- **Callback Signature**:

  ```tsx
  (event: SpineticChangeEvent) => void
  ```

- **`previous`**: Object containing information about the previous state.

  - `index`: The index of the previously active item.
  - `remainingIndexes`: The number of remaining items in the carousel.
  - `totalItems`: The total number of items in the carousel.

- **`current`**: Object containing information about the current state.
  - `index`: The index of the currently active item.
  - `remainingIndexes`: The number of remaining items in the carousel.
  - `totalItems`: The total number of items in the carousel.

### Example Usage:

```tsx
function App() {
  const exampleItems = [
    "content-1",
    "content-2",
    "content-3",
    "content-4",
    "content-5",
    "content-6",
    "content-7",
    "content-8",
  ];

  const handleChange = (event: SpineticChangeEvent) => {
  console.log('Previous State:', event.previous);
  console.log('Current State:', event.current);
};

  return (
    <Spinetic change={handleChange}>
      {exampleItems.map((content, index) => (
        <SpineticItem key={index}>
          <div
            style={{ height: 200, width: 250, background: "blue", margin: 10 }}
          >
            {content}
          </div>
        </SpineticItem>
      ))}
    </Spinetic>
  );
}

default App;

```

The SpineticChangeEvent object is used within the change callback function, offering a structured way to access details about the previous and current states of the carousel during a change event.

```tsx
interface SpineticChangeEvent = {
  previous: {
    index: number;
    remainingIndexes: number[];
    totalItems: number;
  };
  current: {
    index: number;
    remainingIndexes: number[];
    totalItems: number;
  };
};
```

Feel free to explore the various features and configurations available. Utilize the Spinetic playground for a customized carousel experience tailored to your preferences. Explore and enjoy!

<div class="nav-previous-next-idx">
[< Getting Started](?path=/docs/pages-getting-started--documentation)

[Playground >](?path=/docs/pages-playground--documentation)
</div>

<br /><br /><br /><br />